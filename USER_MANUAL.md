# KaiZen Flow — User Manual 📘

KaiZen Flow คือแอป task management สำหรับคนที่มีสมาธิสั้น (ADHD) ออกแบบตามหลัก Kaizen — ทำทีละนิด ไม่ต้องสมบูรณ์แบบ

---

## 🧠 Core Concept

แทนที่จะจัดตารางเวลาตายตัว (Calendar Blocking) ที่มักล้มเหลวกับ ADHD — KaiZen Flow ใช้ระบบ **Energy Bucket** จัดงานตาม "ประเภทพลังงาน" ไม่ใช่เวลา

---

## 📥 Brain Dump Zone

พื้นที่สำหรับ "เท" ทุกอย่างออกจากหัว

1. **พิมพ์** task ในช่อง → กด **Enter** หรือปุ่ม **+**
2. กด **▼** เพื่อเปิดตัวเลือกเพิ่มเติม:
   - **⏱️ เวลาประมาณ** — เลือก 5m / 15m / 25m / 30m / 45m / 1h / 1h30m
   - **⚡ พลังงาน** — Low / Medium / High
3. Task ใหม่จะไปอยู่ที่ **Brain Dump** bucket โดยอัตโนมัติ

---

## 🔥 Energy Buckets

ลาก task จาก Brain Dump ไปใส่ bucket ที่เหมาะสม:

| Bucket | Emoji | ไว้ทำ... |
|--------|-------|----------|
| **Urgent** | 🔥 | ไฟลนก้น — ต้องทำวันนี้! |
| **Deadline** | 📅 | กันไว้ก่อน — ใกล้ถึงกำหนด |
| **Admin** | 📥 | น่าเบื่อ — งานบริหาร |
| **Creative** | ✨ | สนุก — งานใช้ไอเดีย |

**วิธีจัดการ task:**
- 🖱️ **ลาก** task ไปยัง bucket อื่นเพื่อย้าย
- ⭐ กด **Star** เพื่อตั้ง Daily Highlight (งานสำคัญสุดของวัน — เลือกได้แค่ 1)
- ✅ กด **Check** เพื่อ mark complete → ได้ confetti! 🎉
- 🗑️ กด **Trash** เพื่อลบ

---

## 🔥⚡🐢 Priority Icons

กดปุ่ม priority บน task card เพื่อ cycle ผ่าน:

| Icon | ความหมาย |
|------|----------|
| 🔥 **Fire** | ด่วน + สำคัญ |
| ⚡ **Bolt** | Quick Win (<5 นาที) |
| 🐢 **Turtle** | Deep Work (ต้องใช้เวลา) |
| ⭕ **None** | ยังไม่กำหนด |

---

## 🏃 Sprint Mode

กด **Sprint** บน bucket → เปิดโหมดโฟกัส full-screen:

1. **Count-up Timer** — จับเวลาจริง ไม่ใช่ countdown
2. **Reality Check** — ถ้าตั้ง estimated duration ไว้ จะเห็น:
   - **แถบเปรียบเทียบ**: ประเมิน vs ใช้จริง
   - **สถานะ**: 🏎️ เร็วกว่าที่คิด / 🎯 ตรงเป๊ะ / ⏰ นานกว่าที่คิด / 🐢 เกินไปมาก
3. **Milestones** — เมื่อถึง 15, 25, 45, 60, 90 นาทีจะมีข้อความให้กำลังใจ
4. กด **Stop Sprint** → บันทึก Kaizen Log

---

## 📝 Kaizen Log

หลังจบ Sprint จะเปิด Kaizen Log:

- เลือก **Mood**: 🔥 Flow / 😐 Okay / 😫 Drained
- **Reality Check**: ดูว่าประเมินเวลาแม่นแค่ไหน
- จด **Notes** สั้นๆ — ทำอะไรไปบ้าง
- กด **บันทึก** → ข้อมูลเก็บเข้า DB เพื่อวิเคราะห์ pattern

---

## 🅿️ Distraction Parking Lot

ปุ่มกลมสีน้ำเงิน **มุมซ้ายล่าง** — จดความคิดที่แทรกเข้ามาโดยไม่หลุดโฟกัส!

1. กดปุ่ม 🅿️
2. พิมพ์ความคิดที่แทรก เช่น "ต้องซื้อนม"
3. กด **Send** → ไปอยู่ใน Brain Dump อัตโนมัติ
4. **ไม่เปลี่ยนหน้า** ไม่ปิด Sprint — ทำงานต่อได้เลย!

Badge สีแดงจะแสดงจำนวน items ที่จอดอยู่

---

## 🛡️ Guard Rails

ระบบป้องกันการทำงานหนักเกินไป:

- ⚠️ **16:00** (Emergency Alert) — ถ้ายังมี Daily Highlight ที่ยังไม่เสร็จ
- 🛑 **21:00** (Hard Stop) — แนะนำให้หยุดพัก

---

## 🔌 API & MCP

สำหรับ AI agents (Cursor, Claude) หรือ automation tools:

### REST API
```
Base URL: http://<your-server>/api
Header: X-API-Key: <your-key>
```

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/tasks` | ดึง tasks ทั้งหมด |
| POST | `/tasks` | สร้าง task ใหม่ |
| PUT | `/tasks/:id` | แก้ไข task |
| DELETE | `/tasks/:id` | ลบ task |
| POST | `/sprints/start` | เริ่ม sprint |
| POST | `/sprints/stop` | หยุด sprint |
| GET | `/events` | SSE real-time events |

### MCP (Model Context Protocol)
```
Endpoint: http://<your-server>/mcp
Transport: Streamable HTTP
```

**10 Tools**: `list_tasks`, `create_task`, `update_task`, `complete_task`, `delete_task`, `start_sprint`, `stop_sprint`, `get_active_sprint`, `create_kaizen_log`, `get_health`

📄 รายละเอียดเพิ่มเติมดูที่ [API_MANUAL.md](API_MANUAL.md)

---

## 🖥️ MCP Config ตัวอย่าง

### Cursor
```json
{
  "mcpServers": {
    "kaizen-flow": {
      "url": "http://<your-server>/mcp",
      "headers": {
        "X-API-Key": "<your-key>"
      }
    }
  }
}
```

---

## 📊 Stats & Review

กดปุ่ม 📊 ที่ Header → เปิดดู:

- Sprint history
- Mood distribution
- ระยะเวลาทำงานรวม

---

## 🚀 Deploy

ดูขั้นตอน deploy ที่ [DEPLOY_CHECKLIST.md](DEPLOY_CHECKLIST.md)

---

*KaiZen Flow • Follow your energy, respect the guard rails 🧘*
